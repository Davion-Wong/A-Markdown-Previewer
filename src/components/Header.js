import React from "react";

function Header() {
  return (
    <header id="header">
      <h1>Markdown Previewer</h1>
      <p>Enter your Markdown text on the left, and see the HTML output on the right!</p>
    </header>
  )
}

export default Header;